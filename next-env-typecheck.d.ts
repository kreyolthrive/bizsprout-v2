// Typecheck-only env definitions (avoid referencing Next's generated route types)
// Provide minimal JSX definitions via React types included in tsconfig.typecheck.json
// Do NOT reference `next` types here.

// You can add any global ambient declarations for typecheck-only needs below.
